var express = require('express');
var router = express.Router();
var request = require('request');

/* GET home page. */
router.get('/', function(req, res, next) {
//TODO: Fill here with the app page rendering.
});

module.exports = router;
